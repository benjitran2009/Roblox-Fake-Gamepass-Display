# Roblox-Fake-Gamepass-Display
Fake owning all the gamepasses in a game.

Code:
```window.alert('Fake Gamepass Display by benjitran2009#0001')
var myClasses = document.getElementsByClassName("PurchaseButton btn-buy-md btn-full-width rbx-gear-passes-purchase");

for (var i = 0; i < myClasses.length; i++) {
  myClasses[i].outerHTML = "<h5>Owned</h5>";
  }var myClasses = document.getElementsByClassName("PurchaseButton btn-buy-md btn-full-width rbx-gear-passes-purchase");

for (var i = 0; i < myClasses.length; i++) {
  myClasses[i].outerHTML = "<h5>Owned</h5>";
  }var myClasses = document.getElementsByClassName("PurchaseButton btn-buy-md btn-full-width rbx-gear-passes-purchase");

for (var i = 0; i < myClasses.length; i++) {
  myClasses[i].outerHTML = "<h5>Owned</h5>";
  }var myClasses = document.getElementsByClassName("PurchaseButton btn-buy-md btn-full-width rbx-gear-passes-purchase");

for (var i = 0; i < myClasses.length; i++) {
  myClasses[i].outerHTML = "<h5>Owned</h5>";
  }var myClasses = document.getElementsByClassName("PurchaseButton btn-buy-md btn-full-width rbx-gear-passes-purchase");

for (var i = 0; i < myClasses.length; i++) {
  myClasses[i].outerHTML = "<h5>Owned</h5>";
  }var myClasses = document.getElementsByClassName("PurchaseButton btn-buy-md btn-full-width rbx-gear-passes-purchase");

for (var i = 0; i < myClasses.length; i++) {
  myClasses[i].outerHTML = "<h5>Owned</h5>";
  }
